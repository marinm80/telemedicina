# Logs y caches
*.log
.DS_Store
.phpunit.result.cache
/.phpunit.cache
Thumbs.db

# VS Code / IDEs
/.idea
/.vscode
/.cursor/
/.zed

# Backend specific (Laravel)
/backend/.env
/backend/.env.backup
/backend/.env.production
/backend/vendor/
/backend/public/storage
/backend/public/build/
/backend/public/hot
/backend/storage/*.key
/backend/storage/pail/
/backend/auth.json
/backend/_ide_helper.php

# Frontend specific (Vue)
/frontend/node_modules/
/frontend/dist/
/frontend/.env.local
/frontend/.env.*.local
/frontend/.pnpm-store/

# Temporal / scratch
/scratch/

# Contiene secretos reales de producción (APP_KEY, passwords de DB) —
# nunca subir esto a un repo público.
/docs/DEPLOY_COOLIFY.md

# Mockups/referencias de diseño (capturas, exports .dc.html) — material de
# trabajo, no código del proyecto; una vez implementado el componente real
# y documentado en UI_COMPONENTS.md/UI_PROTOTYPE.md, no hace falta versionarlo.
/docs/diseno_*/
/docs/*.zip
